[package]
name = "linux-monitor"
version = "0.1.0"
edition = "2021"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

# 零依赖设计：数据采集全部走 /proc 与系统命令行（systemctl/journalctl/crontab），
# 磁盘空间用 extern "C" 直接声明 statvfs，不引入任何第三方 crate
[dependencies]

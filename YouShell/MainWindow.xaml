<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="YouShell.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YouShell"
    xmlns:term="using:YouShell.Terminal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="YouShell">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 连接栏（垂直切片用，后续替换为 WelcomePage 主机列表） -->
        <StackPanel Orientation="Horizontal" Spacing="8" Padding="12">
            <TextBox x:Name="HostBox" PlaceholderText="主机/IP" Width="180" />
            <TextBox x:Name="PortBox" PlaceholderText="端口" Text="22" Width="64" />
            <TextBox x:Name="UserBox" PlaceholderText="用户" Width="120" />
            <PasswordBox x:Name="PassBox" PlaceholderText="密码" Width="140" />
            <Button x:Name="ConnectBtn" Content="连接" Click="ConnectBtn_Click" />
            <Button x:Name="LocalBtn" Content="本地CMD" Click="LocalBtn_Click" />
            <TextBlock x:Name="StatusText" Text="未连接" VerticalAlignment="Center" />
        </StackPanel>

        <!-- 终端原生 HWND 覆盖区域 -->
        <term:TerminalControl x:Name="Terminal" Grid.Row="1" />
    </Grid>
</Window>

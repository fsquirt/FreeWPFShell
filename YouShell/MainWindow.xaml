<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="YouShell.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YouShell"
    xmlns:views="using:YouShell.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="YouShell">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 沉浸式标题栏：内容延伸到标题栏区域，此元素作为可拖拽的标题栏 -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="48">
            <TextBlock Text="YouShell" FontSize="14" VerticalAlignment="Center" Margin="16,0,0,0" />
        </Grid>

        <TabView
            x:Name="SessionTabs"
            Grid.Row="1"
            IsAddTabButtonVisible="False"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            TabCloseRequested="SessionTabs_TabCloseRequested">
            <TabView.TabItems>
                <TabViewItem x:Name="WelcomeTab" Header="首页" IsClosable="False">
                    <views:WelcomePage x:Name="WelcomePageControl" />
                </TabViewItem>
            </TabView.TabItems>
        </TabView>
    </Grid>
</Window>
